"""
Abstraction : It means a set of layers 
              meeans like keybord + mouse + cpu + monitor ==> PC 
              then keybord and others are abstraction .. means breaks PC into some pieces called abstraction
              We don't need any information that how it works

Encapsulation : Encapsulation in Python is the process of wrapping up variables and methods into a single entity.
                In programming, a class is an example that wraps all the variables and methods defined inside it. 

"""
